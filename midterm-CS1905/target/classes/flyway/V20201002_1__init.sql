Create table person (
    id serial not null,
    firstname varchar default (128),
    lastname varchar default (128),
    city varchar default (128),
    phone varchar default (128),
    telegram varchar default (128)
);
